struct person {
  int id;
  int age;
};

int main(){
    struct person* p = alloc(struct person);
    struct person* p2 = alloc(struct person);

    (*p).id = 1;
    (*p).age = 25;

    (*p).age -= 2;

    p2->id = 1;
    p2->age = 25;

    p2->age -= 2;

    return p2->age + (*p).age;
}
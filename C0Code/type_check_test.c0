
struct MyStruct {
    int mostSignificant;
    bool test;
    int[] arr;
};

// TODO: Functions

int calculate()
{
    return 0;
}

void main()
{
    //bool b = 5 && 3; // ERROR!
    //int b = !(5++); // ERROR!
    //b += false; // ERROR!
    //int a;
    //bool d = true;
    //int k = 5 + 3;
    //int c = 0;
    //int[] A = alloc_array(int, 10);
    //A[0] = 35;
    //for (int i = 0; i < 5; i++){
    //    A[i] = 5;
    //}    
    //int* ptr = alloc(int);
    //*ptr = k;
    //struct MyStruct* s = alloc(struct MyStruct);
    //struct MyStruct* s2;
    //(*s).test = true;
    //s->test = true;
    //s->arr[0] = 35;

    //int i = calculate();

}
